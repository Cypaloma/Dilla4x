# Most features are now defined in info.json (modern QMK)
# Only override defaults here if needed

# VIA Support (add layers for VIA compatibility in keymap)
VIA_ENABLE = yes

# Link Time Optimization (reduce firmware size)
LTO_ENABLE = yes
